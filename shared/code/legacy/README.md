# Legacy developer utilities

Các file ở đây được giữ để tra cứu lịch sử. Chúng **không phải** developer API
canonical. Proto/Wire generation dùng root `make generate`; local runtime dùng
root `make up/status/logs/down`.
