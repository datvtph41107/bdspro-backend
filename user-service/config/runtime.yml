rpc:
  user:
    address: "localhost:8201"
  auth:
    address: "localhost:8216"
  organization:
    address: "localhost:8207"
  hub:
    address: "localhost:8280"
  bdspro:
    address: "localhost:8202"
  notification:
    address: "localhost:8204"
  payment:
    address: "localhost:8205"
  chat:
    address: "localhost:8208"
  crm:
    address: "localhost:8206"

server:
  name: user-service
  port: 8001
  tcp_port: 8201

database:
  host: localhost
  port: 5432
  name: user_service
  user: postgres
  password: ""
  dsn: ""
  max_open_conns: 50
  max_idle_conns: 10
  conn_max_lifetime: 5m


logging:
  level:
    root: INFO
    org.springframework.web: DEBUG
    org.hibernate: ERROR


    

service:
  name: "user service"
  feature: "Enabled only for Service User"

redis:
  host: localhost:6379
  # host: localhost:6379
  pass: ""
  db: 0


oauth:
  google:
    client_id: 768102211675-0d4sd4dpormuo2n2gn1vgvs3h8ag5kr9.apps.googleusercontent.com
    client_secret: ""
    redirect_uri: http://localhost:3001/ban-do/oauth-callback
  
  facebook:
    client_id: 2029655954220379
    client_secret: ""
    redirect_uri: http://localhost:8000/user/oauth/facebook/callback
  
  zalo:
    app_id: 2829833978864922978
    secret_key: ""
    redirect_uri: http://localhost:8000/user/oauth/zalo/callback
    permission_url: https://oauth.zaloapp.com/v4/permission?

key:
  jwt: ""
jwt:
  key-generate: ""
  tokenPrefix: Bearer
  tokenExpirationAfterDays: 14
  accessExpAfterMinutes: 150 # access token hết hạn 100 giờ
  refreshExpAfterMinutes: 129600 # refresh token có hạn là 3 tháng
  listPermit:
  authorizationHeader: Authorization

sms:
  endpoint: "https://api.yoursales.vn/api/public/sms/send"
  provider: "twilio"  # or other SMS providers
  account_sid: "your_twilio_account_sid"
  auth_token: ""
  from_number: "+1234567890"
  template: "Mã xác thực của bạn là {OTP}. Vui lòng không chia sẻ OTP cho bất kỳ ai."

outbound_messaging:
  enabled: false

zns:
  template_id: 497058
  otp_template_id: 497058  # Template ID riêng cho OTP
  access_token: ""
  refresh_token: ""

otp:
  max-times-resend: 5
  max-times-otp-enter: 3 #số lần nhập OTP
  expired-after-seconds: 600 #10p hết hạn otp
  lock-verify-after: 900 # khóa tài khoản 15p nếu sai OTP/gửi OTP 5 lần
  lock-send-after: 30 # lần gửi OTP tiếp theo trong request hiện tại sau 60s
  lock-request-after: 30 # lần gửi OTP giữa 2 lần request là 1 phút (thay đổi từ 5p → 1p)
  limit-otp-device: 10
  limit-otp-ip: 100
  cooldown-seconds: 300
  ip-cooldown-seconds: 900

grpc:
  organization:
    host: :8207
  group:
    host: :8207
  payment:
    host: :8205
  auth:
    host: :8202
  notification:
    host: :8208

admin:
  username: admin
  password: ""
  email: admin@example.com
  full_name: Administrator

cskh:
  # Danh sách profile ID của đội chăm sóc khách hàng BDSPro
  # Những user này sẽ được thêm vào nhóm chat "Hỗ trợ khách hàng" khi user mới đăng ký
  team_ids: []  # Ví dụ: [1, 2, 3] - cần cập nhật với ID thực tế của đội CSKH
