# Historical Payment event migration

Đây là bản lưu lịch sử của migration từng nằm ngoài CRM canonical migration
path. Schema tương đương hiện được sở hữu bởi:

`infra/db/migrate_v2/000025_payment_event_inbox.{up,down}.sql`

Không chạy trực tiếp các file lịch sử này.
