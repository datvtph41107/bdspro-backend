# CRM migration hotfixes

Các file trong thư mục này **không thuộc versioned migration authority** và
không được `make migrate` tự chạy. Chúng chỉ là recovery SQL lịch sử cho một
trạng thái database cụ thể.

Canonical CRM migration history nằm tại `infra/db/migrate_v2/`.
